The (newly written) compile_and_run script compiled them, ran them, and collected the output.

The (newly written) csv2json program makes it possible to keep score in a spreasheet rather
than having to edit the student.json files
